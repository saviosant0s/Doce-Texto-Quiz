# Gera a versão para navegador (celular) com pygbag e publica no GitHub Pages.
# Requer: Settings → Pages → Source: "GitHub Actions".
name: Publicar versão web

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pygbag==0.9.3
      - run: python -m pygbag --build --title "Doce Texto Quiz" ppa_final
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ppa_final/build/web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
